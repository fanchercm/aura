# tests/reference — reference tier (release/blocking)

Cross-validation against trusted external truth: the published PbSO₄ round-robin
structure, and golden-file captures from GSAS-II on the SNAP/NAC/FAP data.
Compared within combined uncertainty. Gated behind release tags / manual
dispatch — never per-PR.

(Holds `golden/` captures + a capture-procedure note once Phase 4 refines real data.)
