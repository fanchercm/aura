# tests/campaign — campaign tier (nightly, non-blocking)

Multi-state / sequential / parametric campaign behaviour: sequential ordering
independence, parametric lattice laws across a series, phase appearance/
disappearance, dynamic strain, multibank texture. Slower, real-data-scale.
Reported nightly; not a per-PR gate.

(Empty until the parametric engine and domain modules land — Phases 5–7.)
